spring:
    datasource:
        url: jdbc:postgresql://postgres:5432/identity_db
        username: postgres
        password: postgres

    jpa:
        hibernate:
            ddl-auto: validate
        show-sql: false

    flyway:
        enabled: true

server:
    port: 8080

logging:
    level:
        root: INFO
